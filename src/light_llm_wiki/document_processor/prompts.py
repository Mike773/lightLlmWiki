ABBREVIATIONS_PROMPT = """\
Извлеки из документа все аббревиатуры (сокращения), которые в нём упоминаются.

Верни только сами аббревиатуры — список строк. Никаких расшифровок,
описаний или пояснений. Не включай слова, которые не являются аббревиатурами.

Документ:
\"\"\"
{content}
\"\"\"
"""


EXPANSION_PROMPT = """\
В документе ниже упоминается аббревиатура "{name}".

Расшифровка аббревиатуры — это последовательность полных слов, по начальным
буквам которых она составлена. Пример: для "API" расшифровка — "Application
Programming Interface" (или, в русском варианте, "Программный Интерфейс
Приложения"); для "СУБД" — "Система Управления Базами Данных".

Сделай две вещи:

1. expansion — сама расшифровка строкой, без пояснений и контекста.
   Не возвращай предложение, в котором аббревиатура упомянута: расшифровка —
   это именно набор слов, разворачивающий аббревиатуру.

2. quote — буквальная цитата из документа (символ-в-символ, без перефраза,
   без многоточий, без сокращений), в которой эта расшифровка приведена
   рядом с аббревиатурой. Это короткий фрагмент исходного текста (одно
   предложение или его осмысленная часть), который непосредственно
   подтверждает, что расшифровка взята из документа, а не придумана.

Условия:
- Оба поля должны быть либо одновременно заполнены, либо одновременно null.
- Если в документе расшифровки нет — верни expansion=null И quote=null.
  НЕ используй внешние знания и не додумывай (даже если ты знаешь
  общепринятую расшифровку).
- Цитата ОБЯЗАНА быть точной подстрокой документа. Не переписывай и не
  «нормализуй» текст: сохрани оригинальный регистр, пунктуацию и пробелы.
- Если расшифровка явно не приведена в документе рядом с аббревиатурой
  (а лишь подразумевается или известна тебе из общих знаний) — верни оба
  поля как null.

Документ:
\"\"\"
{content}
\"\"\"
"""


ENTITIES_PROMPT = """\
Извлеки из документа список предметных сущностей: терминов, объектов,
концепций, систем, процессов и иных именованных понятий, которым в документе
даётся (или может быть дано) самостоятельное определение или описание.

Верни только имена сущностей — список строк. Никаких описаний, расшифровок
или пояснений.

Критерий отбора. Сущность должна:
- иметь самостоятельный смысл в предметной области документа;
- быть кандидатом на собственное определение (можно сформулировать «X — это …»
  именно про неё, а не про более широкое понятие, частью которого она является).

Что НЕ включать:
- Аббревиатуры (сокращения). Они извлекаются отдельным стейджем. Конкретно
  следующие имена — это аббревиатуры, и их в результирующем списке быть
  не должно ни в каком виде:
{abbreviations_to_exclude}
- Не-предметные обобщения и абстрактные родовые категории — слова, которые
  сами по себе не специфичны, а лишь обозначают широкий класс или общее
  понятие (например, «анализ», «данные», «процесс», «система», «информация»,
  «событие»). Их извлекать не нужно — извлекаются их конкретные подвиды,
  если они в документе названы (например, не «анализ», а «когортный
  анализ»; не «процесс», а конкретно поименованный процесс).
- Подстроки и фрагменты других сущностей. Если в списке уже есть «Когортный
  анализ», то отдельно «анализ» или «когорта» включать не нужно.
- Атрибуты, свойства и характеристики других сущностей, а не самостоятельные
  объекты (например, «бизнес-активность», «атомарная единица»,
  «агрегированные показатели» — это описание свойств, а не сущности).
- Общеупотребительные слова, не являющиеся специфичными сущностями
  предметной области.

Документ:
\"\"\"
{content}
\"\"\"
"""


ENTITY_DESCRIPTION_PROMPT = """\
В документе ниже упоминается сущность "{name}".

Извлеки из документа определение или описание этой сущности — то, что
объясняет, чем она является, как устроена или для чего предназначена.
Верни только описание, без пояснений и комментариев от себя.

Условия:
- Описание бери только из самого документа. Если его там нет — верни null.
- Не используй внешние знания и не додумывай.
- Если сущность лишь упомянута в документе, но не описана и не определена,
  верни null (даже если ты знаешь, что это такое).
- Если в описании используешь любую аббревиатуру из списка ниже — пиши её
  ровно как в списке, без расшифровки в скобках, без раскрытия. Если для
  аббревиатуры в списке указано «расшифровка не найдена в документе» —
  упоминай только саму аббревиатуру, не разворачивай её по своему знанию.

Для справки ниже перечислены аббревиатуры, упомянутые в документе, с их
расшифровками — используй их, чтобы корректно интерпретировать упоминания
аббревиатур внутри текста. Сами эти аббревиатуры описывать в этом ответе
не нужно.

Аббревиатуры, упомянутые в документе:
{abbreviations_block}

Документ:
\"\"\"
{content}
\"\"\"
"""


RELATIONS_PROMPT = """\
В документе описываются сущности и связи между ними. Дан список сущностей
из документа (с id и кратким описанием).

Сейчас рассматривается сущность с id = {source_id}: "{source_name}".

Найди все остальные сущности из списка ниже, с которыми "{source_name}"
имеет смысловую связь, прямо подтверждаемую содержимым документа. Для
каждой такой связи верни:
- target_id: id связанной сущности из списка
- relation_type: короткое имя типа связи на русском в нижнем регистре,
  одной-двумя словами через пробел или дефис (например: "содержит",
  "использует", "часть", "относится к", "принадлежит", "зависит от").

Условия:
- Связь должна явно следовать из текста документа. Не используй внешние
  знания и не додумывай.
- Не возвращай связь сущности с самой собой.
- Между одной и той же парой сущностей должна быть максимум одна связь —
  направление не различает: A↔B и B↔A считаются одной и той же связью.
  Выбери один наиболее точно описывающий её тип и одно направление.
- Если в relation_type используешь аббревиатуру из списка ниже — пиши её
  ровно как в списке, без расшифровки. Если расшифровка для аббревиатуры
  не найдена в документе — не разворачивай её по своему знанию.
- Если связей нет — верни пустой список.

Аббревиатуры, упомянутые в документе:
{abbreviations_block}

Список сущностей документа (id — name — краткое описание):
{entities_block}

Документ:
\"\"\"
{content}
\"\"\"
"""


RELATION_DESCRIPTION_PROMPT = """\
В документе описана связь между двумя сущностями.

Источник: "{source_name}"
Цель: "{target_name}"
Тип связи: "{relation_type}"

Сделай две вещи:

1. description — кратко опиши, в чём заключается эта связь по содержанию
   документа: чем сущности связаны, как именно проявляется связь. Опиши
   своими словами, опираясь только на документ. Никаких внешних знаний.

2. quote — приведи буквальную цитату из документа, в которой эта связь
   упомянута. Цитата должна быть точной (символ-в-символ), без перефраза,
   без многоточий, без сокращений. Это короткий фрагмент исходного текста
   (одно предложение или его осмысленная часть), который непосредственно
   подтверждает связь.

Условия:
- Описание и цитату бери только из самого документа.
- Если связь в документе фактически не подтверждается — верни оба поля
  как null. Не выдумывай ни описание, ни цитату.
- Цитата обязана быть подстрокой документа. Не переписывай и не
  «нормализуй» текст: сохрани оригинальный регистр и пунктуацию.
- Если в description используешь любую аббревиатуру из списка ниже —
  пиши её ровно как в списке, без расшифровки в скобках, без раскрытия.
  Если для аббревиатуры в списке указано «расшифровка не найдена в
  документе» — упоминай только саму аббревиатуру, не разворачивай её
  по своему знанию.

Аббревиатуры, упомянутые в документе:
{abbreviations_block}

Документ:
\"\"\"
{content}
\"\"\"
"""


WIKI_ENTITY_SUMMARY_PROMPT = """\
Сформируй короткий summary-абзац для wiki-страницы сущности "{name}".

Объём: ровно столько, сколько хватает данных, до 5 предложений в одном
абзаце. Лучше короче, чем добавлять неподтверждённое. Если данных хватает
только на одно предложение — пиши одно предложение.

Что должен передать summary:
- Чем по сути является эта сущность (её определение) — только если это
  следует из переданного описания.
- Её роль в предметной области направления — только если это явно
  следует из описания и/или связей.
- Ключевые связи с другими сущностями — упоминай только тех соседей,
  чьи имена дословно встречаются в блоке связей ниже, и только те типы
  связей, которые там перечислены.

Жёсткие условия:
- Опирайся ТОЛЬКО на данные ниже: описание сущности, её связи с другими
  сущностями (с описаниями и цитатами), список упоминающих документов.
- Не используй внешние знания и не додумывай факты.
- Не дублируй разделы, которые уже будут на странице (полный список связей
  и список документов). Summary — это связный текст, не перечисление.
- Если в summary используешь любую аббревиатуру из списка ниже — пиши её
  ровно как в списке, без расшифровки в скобках, без раскрытия. Если для
  аббревиатуры указано «расшифровка не найдена в документе» — упоминай
  только саму аббревиатуру, не разворачивай её по своему знанию.
- Пиши на русском, в нейтральном описательном стиле.

Fallback. Если описание не зафиксировано (или маркер "(описание не
зафиксировано)"), И в блоке связей нет ни одной связи с непустым
описанием или цитатой, И список документов пуст или не несёт полезной
информации — верни в поле summary РОВНО строку:

Описания не найдено.

Без точек в конце сверх указанной, без пояснений, без перечислений, без
попыток обобщения. Любая попытка «всё-таки что-то написать» в этом
случае считается галлюцинацией и запрещена.

Имя сущности: {name}
Тип: {type}
Описание: {description}

Аббревиатуры направления (имя — расшифровка):
{abbreviations_block}

Связи этой сущности (имя соседа — тип связи — описание — цитата):
{relations_block}

Упоминается в документах:
{documents_block}
"""


WIKI_DIRECTION_OVERVIEW_PROMPT = """\
Сформируй обзорный текст для главной wiki-страницы направления
"{direction_name}".

Объём: до 7 связных абзацев, но ровно столько, сколько действительно
следует из переданных данных. Лучше один короткий абзац по фактам, чем
семь абзацев с домыслами. Без заголовков и markdown — только сам текст.

Что должен передать обзор:
- О чём это направление по предметной сути — только по совокупности
  переданных сущностей и связей.
- Ключевые сущности и их роли — прозой, не перечислением. Упоминай
  только сущности, дословно перечисленные в блоке сущностей ниже.
- Основные связи между сущностями — только те, что перечислены в блоке
  связей. Тип связи указывай так, как написан в данных.
- Какие документы лежат в основе знания, опираясь на их **переданные**
  заголовки и фрагменты содержимого. Не строй догадок о содержании
  документа по одному только заголовку: если фрагмент пуст или
  неинформативен, ограничься констатацией факта наличия документа.

Жёсткие условия:
- Опирайся ТОЛЬКО на данные ниже. Не используй внешние знания.
- Не выдумывай связей, которых нет в списке.
- Если в обзоре используешь любую аббревиатуру из списка ниже — пиши её
  ровно как в списке, без расшифровки в скобках, без раскрытия. Если для
  аббревиатуры указано «расшифровка не найдена в документе» — упоминай
  только саму аббревиатуру, не разворачивай её по своему знанию.
- Пиши на русском, в нейтральном описательном стиле.

Fallback. Если в направлении нет ни сущностей с описаниями, ни связей
с описаниями, ни документов с содержательными фрагментами — верни в
поле overview РОВНО строку:

Описание направления не найдено.

Без точек в конце сверх указанной, без пояснений и попыток обобщения.

Направление: {direction_name}
Описание направления: {direction_description}

Аббревиатуры направления (имя — расшифровка):
{abbreviations_block}

Сущности направления (имя — тип — описание):
{entities_block}

Связи направления (источник — цель — тип — описание):
{relations_block}

Документы направления (название и фрагмент содержимого):
{documents_block}
"""
