QUESTION_ABBREVIATIONS_PROMPT = """\
Из вопроса ниже извлеки все аббревиатуры (сокращения), которые в нём
упоминаются.

Верни только сами аббревиатуры — список строк. Никаких расшифровок,
описаний или пояснений. Не включай слова, которые не являются аббревиатурами.

Вопрос:
\"\"\"
{question}
\"\"\"
"""


QUESTION_ENTITIES_PROMPT = """\
Из вопроса ниже извлеки список именованных сущностей: терминов, объектов,
концепций, систем, процессов, о которых вопрос — то, что предположительно
есть в базе знаний и о чём задаётся вопрос.

Верни только имена — список строк, как они звучат в вопросе (можно в
нормальной форме). Никаких описаний.

Что НЕ включать:
- Аббревиатуры. Они извлекаются отдельно. Конкретно следующие имена —
  это аббревиатуры, и их в результирующем списке быть не должно:
{abbreviations_to_exclude}
- Не-предметные обобщения и абстрактные родовые категории (например:
  «анализ», «данные», «процесс», «система», «информация», «событие»).
- Подстроки и фрагменты других сущностей.
- Атрибуты, свойства и характеристики.
- Общеупотребительные слова, не являющиеся специфичными сущностями.

Если в вопросе нет именованных сущностей — верни пустой список.

Вопрос:
\"\"\"
{question}
\"\"\"
"""


ANSWER_PROMPT = """\
Дан вопрос пользователя и контекст из базы знаний (аббревиатуры,
сущности, связи, документы, wiki-страницы), собранный по сущностям,
упомянутым в вопросе, либо — если по сущностям в базе ничего не нашлось
и был задействован глубокий поиск — по семантической близости к вопросу.

Сделай две вещи:

1. answer — связный ответ на вопрос на русском языке, опирающийся
   ИСКЛЮЧИТЕЛЬНО на контекст ниже. Не используй внешние знания. Если
   часть вопроса контекст не покрывает — прямо скажи, что по этому
   аспекту в базе ничего не найдено. Не выдумывай факты, цифры, имена,
   даты. Не вставляй в ответ id и markdown-ссылки.

   Особое правило про аббревиатуры: не расшифровывай аббревиатуры,
   которых нет в блоке «Аббревиатуры (имя — расшифровка)» — для них в
   базе нет данных. Если в этом блоке у аббревиатуры стоит «(расшифровка
   не зафиксирована)» — также не разворачивай её самостоятельно. Для
   аббревиатур из блока «Не найденные в базе аббревиатуры» и для любых
   аббревиатур из исходного вопроса, отсутствующих в обоих блоках, —
   упоминай только саму аббревиатуру без расшифровки и явно скажи, что
   расшифровки в базе нет.

   Wiki-страницы — обзорные и вторичные относительно блоков «Сущности»
   и «Связи». Используй их, чтобы дополнить ответ, но при противоречии
   между wiki и связями/документами — приоритет у связей и документов.

2. unsupported — список тех конкретных утверждений или подвопросов
   из исходного вопроса, для которых в контексте нет подтверждения.
   Каждый элемент — короткая фраза на русском. Если контекст полностью
   покрывает вопрос — пустой список.

Контекст:

Вопрос: {question}

Аббревиатуры (имя — расшифровка):
{abbreviations_block}

Не найденные в базе аббревиатуры:
{abbreviations_missing_block}

Сущности (имя — тип — описание):
{entities_block}

Связи (источник → цель — тип — описание; цитата из документа):
{relations_block}

Документы (название — содержимое):
{documents_block}

Wiki-страницы (название — содержимое):
{wiki_block}
"""


NARRATIVE_PROMPT = """\
Расскажи на русском, как ты пришёл к ответу на вопрос пользователя — в
формате связного повествования от первого лица («я разобрал ваш вопрос»,
«я нашёл», «я обратился к…», «я свёл вместе»).

Это НЕ структурированный отчёт и НЕ список. Не используй markdown-заголовки,
таблицы, маркированные списки, нумерацию и markdown-ссылки. Только текст
абзацами. Идентификаторы и id страниц не упоминай.

Что должно быть в рассказе:
- какие аббревиатуры и сущности ты выделил из вопроса;
- что из них нашлось в базе (с их сутью своими словами), а что нет;
- какие связи между найденными сущностями ты обнаружил, и какие именно
  фрагменты документов это подтверждают (цитаты можно вставлять как
  внутренние кавычки, но не как «> blockquote»);
- какие документы оказались релевантны;
- если ни сущностей, ни аббревиатур из вопроса в базе не оказалось, но
  был задействован глубокий поиск — отметь, что я обратился к
  wiki-страницам направления (по смысловой близости к вопросу) и прошёл
  по исходным текстам документов направления, отбирая релевантные;
  кратко перескажи, что именно из них пригодилось;
- к какому ответу это привело — и какие части вопроса остались без
  подтверждения в базе.

Если по какому-то понятию или подвопросу база не дала информации, скажи
это явно: «по такому-то аспекту в базе ничего не нашлось».

Если ни одной аббревиатуры и ни одной сущности из вопроса в базе не
оказалось и глубокий поиск тоже ничего не дал — рассказ должен это
честно отразить, без выдумывания фактов.

Опирайся ТОЛЬКО на материалы ниже. Не добавляй внешних знаний и не
дополняй того, чего нет в данных.

Вопрос: {question}

Найденные аббревиатуры (имя — расшифровка):
{abbreviations_found_block}

Не найденные в базе аббревиатуры:
{abbreviations_missing_block}

Найденные сущности (имя — тип — описание):
{entities_found_block}

Не найденные в базе сущности:
{entities_missing_block}

Связи (источник → цель — тип — описание; цитата из документа):
{relations_block}

Документы (название — содержимое):
{documents_block}

Wiki-страницы (название — содержимое):
{wiki_block}

Финальный ответ системы (которым ты должен закончить рассказ, перефразируя
его в общий поток):
{answer}

Подвопросы, оставшиеся без подтверждения:
{unsupported_block}
"""


DEEP_DOC_RELEVANCE_PROMPT = """\
Тебе дан вопрос пользователя и один документ из базы знаний. Реши, есть
ли в этом документе материал, прямо отвечающий на вопрос или его часть.

Верни два поля:

1. relevant — true, если в документе есть конкретные сведения, которые
   помогают ответить на вопрос (целиком или на одну из его частей).
   false, если документ про другое: тематически рядом, но никаких
   фактов, отвечающих на этот вопрос, в нём нет.

   Не помечай документ как relevant только потому, что упомянуты
   созвучные термины — нужны именно сведения по сути вопроса.

2. summary — выжимка ИМЕННО тех фрагментов документа, которые
   отвечают на вопрос: факты, формулировки, числа, имена и цитаты как
   они есть в документе. На русском. Без выводов, обобщений и внешних
   знаний. Если уместно — краткие цитаты в кавычках. Без markdown-заголовков
   и без id.

   Если relevant=false — оставь summary пустой строкой.

Не выдумывай ничего, чего нет в документе. Не упоминай документы или
сущности, которые в этом тексте не названы.

Вопрос: {question}

Документ «{title}»:
\"\"\"
{content}
\"\"\"
"""
